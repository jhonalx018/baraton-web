const CategoriesRx = (state = [], action) => {
  switch (action.type) {
    case 'GET_DATA_CATEGORIES':
      return state;
      break;
    default:
      return state;
      break;
  }
}
;

export { CategoriesRx };
